name: murmur-hook-compact
version: 0.3.0
runtime: hook
binding: on-compaction
execution_mode: blocking
commit_policy: replace-context
description: "Summarises conversation history when the session token threshold is reached."
